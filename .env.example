# Copy to .env. Never commit the real file.
MORNING_ENV=development
MORNING_PORT=8080
POSTGRES_DB=morning
POSTGRES_USER=morning
POSTGRES_PASSWORD=replace-with-a-long-random-password
MORNING_SESSION_SECRET=replace-with-at-least-32-random-bytes
