[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "morning"
version = "0.1.0"
description = "Standalone Morning operational reporting product"
requires-python = ">=3.12"
dependencies = [
  "alembic>=1.16,<2",
  "itsdangerous>=2.2,<3",
  "psycopg[binary]>=3.2,<4",
  "pypdf>=6,<7",
  "sqlalchemy>=2.0,<3",
  "starlette>=0.47,<1",
  "uvicorn>=0.34,<1",
]

[project.optional-dependencies]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.3,<9",
  "ruff>=0.12,<1",
]

[project.scripts]
morning = "morning.cli:main"

[tool.setuptools]
package-dir = {"" = "backend"}

[tool.setuptools.packages.find]
where = ["backend"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
